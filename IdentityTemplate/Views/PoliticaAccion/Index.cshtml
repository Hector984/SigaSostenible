@model IEnumerable<IdentityTemplate.Models.Catalogs.PoliticaAccion>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index nuevo</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombrePoliticaAccion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombrePoliticaAccion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PoliticaAccionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PoliticaAccionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PoliticaAccionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
